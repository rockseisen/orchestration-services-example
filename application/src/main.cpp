#include <iostream>
#include <stdio.h>

#include <orchestration/orchestration.h>

int main() {
    //auto account = Account(42);
    //account.AddMoney(12);
    puts("This is the main application");
    Orchestration();
    return 0;
}
